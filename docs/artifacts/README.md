# Generated Artifacts

This folder is the target home for generated JSON and markdown artifacts.

Target organization:

- `crypto/`
- `pairing/`
- `sports/`
- `simulation/`
- `operations/`

Current repo state still contains many generated artifacts at the top level of `docs/`.
Future cleanup should move generated outputs here while preserving compatibility for existing scripts and operator workflows.
